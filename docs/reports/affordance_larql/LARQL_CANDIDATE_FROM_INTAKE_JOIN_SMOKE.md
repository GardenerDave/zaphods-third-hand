# LARQL Candidate From Intake Join Smoke

Date: 2026-06-29

This report records the next intake join:

`reviewed intake artifact → drafted LARQL candidate artifact → held for candidate review`

## What this join smoke proves

The reviewed intake artifact can be converted into a held candidate draft without promoting the candidate or changing the completed registry.

That means the noisy intake edge can now feed the existing candidate-drafting lifecycle in a structured way.

This is supervised guided capability, not autonomous repo authority and not general intelligence.

## What it does not prove

This join smoke does not prove:

- the candidate is accepted;
- a runtime rule exists;
- a runtime rule should be installed;
- a probe should run;
- arbitrary messy input is solved generally;
- the completed registry should change.

The candidate remains held for supervised candidate review.

## Why the candidate remains held

The review step only hands off to candidate drafting. It does not authorize promotion.

The draft artifact preserves the same authority boundaries as the reviewed intake:

- no model call
- no registry promotion
- no runtime rule creation
- no runtime rule install
- no training data
- no dataset artifact
- no durable memory
- no candidate promotion
- no model weight mutation
- no runtime rule modification
- no automatic failure-to-curriculum capture

## How this bridges reviewed intake into the existing lifecycle

The intake review join smoke proves the pipeline can go one step further without inventing new authority:

1. noisy note
2. held intake candidate scaffold
3. supervised intake review artifact
4. held candidate draft

That handoff is enough for the existing candidate-review lifecycle to inspect later.

## Why the completed registry remains unchanged

This join smoke is a local draft artifact only.

It does not modify the completed rule registry, does not create a new runtime rule, and does not install anything.

The completed registry remains the source of truth for the already-finished LARQL rules.

## Relevant evidence location

- reviewed intake artifact: `.work/larql_intake_reviews/intake_review_001/larql_intake_review.json`
- drafted candidate artifact: `.work/larql_candidate_from_intake/candidate_from_intake_001/larql_candidate_draft.json`

## Next step

The next step is supervised candidate review of the drafted candidate, if the operator wants to continue the lifecycle.

No automatic failure-to-curriculum capture was performed here, and that remains opt-in only.

