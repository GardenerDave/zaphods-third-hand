# Local Agent Task

Re-run the six-source supporting-context task after switching the Gemma-local wrapper to inline a compact read digest into the Aider message instead of sending read-only files through Aider's `--read` path.
