# Local Agent Task

Execute one bounded one-file real-code Aider task to confirm whether accepted edits recover at the smallest practical real-code surface.

Target behavior:
- Edit only one real test file.
- Make a tiny deterministic assertion update.
- Keep bounded timeout and no manager reruns.