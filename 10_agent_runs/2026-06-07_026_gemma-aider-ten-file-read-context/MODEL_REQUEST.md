# Model Request

Make these exact replacements:

- In `TARGET_A.md`, replace `placeholder` with `alpha ok`.
- In `TARGET_B.md`, replace `placeholder` with `beta ok`.
- In `TARGET_C.md`, replace `placeholder` with `gamma ok`.
- In `TARGET_D.md`, replace `placeholder` with `delta ok`.
- In `TARGET_E.md`, replace `placeholder` with `epsilon ok`.
- In `TARGET_F.md`, replace `placeholder` with `zeta ok`.
- In `TARGET_G.md`, replace `placeholder` with `eta ok`.
- In `TARGET_H.md`, replace `placeholder` with `theta ok`.
- In `TARGET_I.md`, replace `placeholder` with `iota ok`.
- In `TARGET_J.md`, replace `placeholder` with `kappa ok`.

Use `REFERENCE.md` only as read-only context. Do not edit it.
