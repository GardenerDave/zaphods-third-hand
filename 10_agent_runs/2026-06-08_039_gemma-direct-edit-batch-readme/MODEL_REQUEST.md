# Model Request

- In `local_harness/README.md`, replace `- Deterministic insert-after and insert-before edits are now manager-routable when the anchor appears exactly once.` with `- Deterministic insert-after and insert-before edits are now manager-routable when each anchor stays unique at the step where it is used.`.
- In `local_harness/README.md`, insert `
- Batched one-file deterministic edits are now manager-routable when each step stays unique.` after `- Deterministic insert-after and insert-before edits are now manager-routable when each anchor stays unique at the step where it is used.`.
- Edit only the listed file.
