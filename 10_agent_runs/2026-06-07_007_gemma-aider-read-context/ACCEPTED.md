# Accepted Artifact

## Accepted result

The `gemma-local` profile can use one small read-only context snapshot while editing one tiny target file.

## Accepted caution

Read-only context still increases prompt size quickly. Keep `--read` inputs trimmed and prefer one short snapshot until larger-context pilots are validated.
