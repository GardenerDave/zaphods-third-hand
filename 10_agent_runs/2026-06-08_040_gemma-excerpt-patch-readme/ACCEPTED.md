# Accepted Output

- Accepted finding: real excerpt SEARCH/REPLACE patch requests need a wider prompt budget than literal single-line deterministic edits.
- Accepted next move: widen the excerpt patch prompt cap and rerun the same README task as the positive control.
