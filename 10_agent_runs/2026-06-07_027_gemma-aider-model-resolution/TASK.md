# Local Agent Task

Use Aider to improve the smaller `icm_call.py` orchestration surface so it can resolve the actual local model id from `/v1/models` when configured with an OpenAI-style alias such as `openai/gemma4`.
