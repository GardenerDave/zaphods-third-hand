# Local Agent Task

Re-run the six-read-context slack-budget probe after adding bundled read inputs to the `gemma-local` Aider wrapper.

The manager goal is to verify that the same logical task that previously failed with six separate read-only files can now complete when the wrapper hands Aider one consolidated read bundle.
