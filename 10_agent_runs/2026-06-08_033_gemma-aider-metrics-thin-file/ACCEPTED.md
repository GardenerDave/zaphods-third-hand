# Accepted Output

- No worker output accepted from this run.
- Accepted finding: even a thin one-file real-code task can time out post-dispatch while remaining within budget and matching the validated tiny-task heuristic.
- Accepted operating rule: treat `validated_shape_match` as a routing hint, not as evidence that a real-code Aider run will complete.
