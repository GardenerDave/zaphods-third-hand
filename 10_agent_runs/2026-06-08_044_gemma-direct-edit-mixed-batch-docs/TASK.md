# Local Agent Task

Validate that one bounded manager batch can combine an excerpt SEARCH/REPLACE patch on one real repo file with a literal deterministic edit on another real repo file, without launching Aider or touching the local endpoint.
